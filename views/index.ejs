<!DOCTYPE html>
<html>
<head>
	<title>Matrimony Nepal</title>
    <link rel="shortcut icon" href="images/marriage.ico" />
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css"> <!-- load bootstrap css -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css"> <!-- load fontawesome -->
    <link href="stylesheets/style.css" rel="stylesheet">
</head>
<body>
<div class="container">

    <div class="jumbotron text-center">
        <i>why bother with boyfriend/girlfriend when there is</i>
        <h1>Matrimony Nepal</h1>

        <p>Login or Register with:</p>

        <a href="/login" class="btn btn-default"><span class="fa fa-user"></span> Login</a>
        <a href="/signup" class="btn btn-default"><span class="fa fa-user"></span> Signup</a>
        <a href="/auth/facebook" class="btn btn-primary"><span class="fa fa-facebook"></span> Facebook</a>
        <a href="/auth/google" class="btn btn-danger"><span class="fa fa-google-plus"></span> Google</a>
    </div>

    <% if (post.length > 0) { %>
        <% for (i = 0; i < post.length; i++) { %>
            <div class="col-sm-6">
                <div class="well">
                    <h4 class="alert alert-success">AVAILABLE</h4>
                    <div id="card-left">
                        <p>
                            <strong>Name : </strong><%= post[i].name %></br>
                            <strong>Gender : </strong><%= post[i].gender %></br>
                            <strong>Age : </strong><%= post[i].age %></br>
                            <strong>Email : </strong><%= post[i].email %></br>
                            <strong>Phone : </strong><%= post[i].phone %></br>
                            <strong>Qualification : </strong><%= post[i].qualification %></br>
                            <strong>Looking for : </strong><%= post[i].lookingFor %></br>
                            <strong>Bio : </strong><%= post[i].bio %></br>
                        </p>
                    </div>
                    <div id="card-right">
                        <img src='<%= post[i]._id %>.png' class="img-rounded" alt="<%= post[i].name %>"  width="150" height="150">
                    </div>
                </div>
            </div>
        <% } %>
    <% } %>

</div> <!--container -->

</body>
</html>